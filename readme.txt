Dependencies:
	Python
	google cloud/APIs enabled
	Flask==3.0.3
	gunicorn=22.0.0
	google-cloud-aiplatform==1.72.0
You can use the command 'pip3 freeze' to check that you have all the necessaru modules'
Save the folder to desired directory.
Use the command 'cd project3' to access the project.
Use the command 'python app_project3.py' to launch the project.



